// def defines a function. It answers with its last expression, so `return` is
// optional -- both of these give the same thing.
//
// Make this print:
//
//   6
//   9

def double(x)
  return x * 2
end

def triple(x)
  x * 3
end

puts(double(3))
// TODO: call triple with 3
