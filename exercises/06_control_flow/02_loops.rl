// foreach walks an array, and with two names it gives the index first and the
// value second.
//
// Make this print:
//
//   0:a
//   1:b
//   3

letters = ["a", "b"]

// TODO: print each index and letter joined by a colon, like 0:a

count = 0
while count < 3
  count = count + 1
end
puts(count)
