// Comparison gives a BOOLEAN. Only false and nil are false in RocketLang, so
// 0 and "" are both true -- worth remembering, it differs from other languages.
//
// Make this print:
//
//   true
//   false
//   true

puts(3 < 4)
puts(3 == 4)
// TODO: print whether 3 is not equal to 4
