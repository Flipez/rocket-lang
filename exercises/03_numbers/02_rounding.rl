// ceil, floor, round and truncate take an optional number of decimal places.
// They answer with a FLOAT, keeping the type they were given.
//
// Make this print:
//
//   2.0
//   1.0
//   1.57

value = 1.567

puts(value.ceil())
puts(value.floor())
// TODO: round to two decimal places
