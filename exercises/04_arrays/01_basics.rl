// An array is indexed from 0. A negative index counts back from the end, and
// a slice takes a range.
//
// Make this print:
//
//   10
//   40
//   [20, 30]

numbers = [10, 20, 30, 40]

puts(numbers[0])
puts(numbers[-1])
// TODO: print the middle two elements, using a slice
